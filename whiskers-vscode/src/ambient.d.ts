// Real types (@types/vscode, @types/ws, node-pty bundled) are installed via
// `npm install`, so no ambient stubs are needed. Intentionally empty — kept as a
// placeholder so the scaffold layout is stable.
export {};
